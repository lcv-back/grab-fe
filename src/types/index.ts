export type User = {
    id?: string;
    email: string;
    fullname: string;
    birthday: string;
    gender: string;
    createdAt?: string;
    updatedAt?: string;
  };

export type Symptom = {
    id?: number;
    name: string;
};